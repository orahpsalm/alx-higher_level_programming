-- Show the score and name colums order numerically if the score >= 10
-- Query that shows the score and name colums order numerically if the score >= 10
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;

-- Show score and name colums order numerically if score >= 10
-- Query that shows the score and name colums ordered numerically if score >= 10
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
